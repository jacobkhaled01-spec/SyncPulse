# 🎓 دليل المناقشة وتوزيع المتحدثين وبنك الأسئلة المتوقعة
### لمشروع نظام المراسلة والاتصالات المتقدم (SyncPulse / SecureTalk)
### مخصص لمناقشة التخرج أمام مدرس مقرر "برمجة خادم وعميل" (Client-Server Programming)
### مصفوفة الأدوار لفريق مكون من (4 متحدثين / 4 مهندسين)

---

## 📌 الفهرس العام لدليل المناقشة

1. [مصفوفة توزيع محاور المقرر على فريق الـ 4 متحدثين](#1-مصفوفة-توزيع-محاور-المقرر-على-فريق-الـ-4-متحدثين)
2. [التوزيع الزمني والتفصيلي لكل متحدث أثناء العرض](#2-التوزيع-الزمني-والتفصيلي-لكل-متحدث-أثناء-العرض)
3. [سيناريو العرض العملي الحي خطوة بخطوة (Live Socket Demo Playbook)](#3-سيناريو-العرض-العملي-الحي-خطوة-بخطوة)
4. [بنك الأسئلة التقنية العميقة لمدرس مقرر خادم وعميل مع الإجابات النموذجية](#4-بنك-الأسئلة-التقنية-العميقة-لمدرس-مقرر-خادم-وعميل)
   - [المحور الأول: مقابس TCP وهندسة التأطير (TCP Sockets & Framing)](#المحور-الأول-مقابس-tcp-وهندسة-التأطير)
   - [المحور الثاني: نماذج التزامن وإدارة خيوط الخادم (Concurrency & Multi-Threading)](#المحور-الثاني-نماذج-التزامن-وإدارة-خيوط-الخادم)
   - [المحور الثالث: بروتوكولات النقل والمقارنة بين TCP و UDP](#المحور-الثالث-بروتوكولات-النقل-والمقارنة-بين-tcp-و-udp)
   - [المحور الرابع: كشف انقطاع الاتصال وإدارة الجلسات (State & Heartbeat)](#المحور-الرابع-كشف-انقطاع-الاتصال-وإدارة-الجلسات)
   - [المحور الخامس: إدارة الذاكرة والأداء والأمان (Memory Safety, SQL & Crypto)](#المحور-الخامس-إدارة-الذاكرة-والأداء-والأمان)
   - [المحور السادس: القرارات المعمارية والبدائل (Architectural Decisions)](#المحور-السادس-القرارات-المعمارية-والبدائل)
5. [نصائح وإرشادات ذهبية لنيل الدرجة الكاملة](#5-نصائح-وإرشادات-ذهبية-لنيل-الدرجة-الكاملة)

---

# 1. مصفوفة توزيع محاور المقرر على فريق الـ 4 متحدثين

تم تقسيم المشروع ومحاور مقرر برمجة خادم وعميل إلى 4 أدوار هندسية متوازنة تبرز كفاءة وتكامل الفريق:

```text
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│             مصفوفة توزيع محاور مقرر "برمجة خادم وعميل" على فريق الـ 4 متحدثين             │
├──────────────────────────┬──────────────────────────┬────────────────────────────────────┤
│ المتحدث الأول            │ المتحدث الثاني           │ المتحدث الثالث                     │ المتحدث الرابع
│ هندسة المقابس والتأطير   │ بروتوكولات الأمان والجلسة│ خادم TCP وقواعد البيانات والمزامنة │ ميديا UDP والذاكرة والبث الهجين
│ (Sockets & Framing)      │ (Auth, Security & State) │ (TCP Server, SQLite & Sync Engine) │ (UDP Relay, Memory & Streaming)
└──────────────────────────┴──────────────────────────┴────────────────────────────────────┴────────────────────────────────────┘
```

---

# 2. التوزيع الزمني والتفصيلي لكل متحدث أثناء العرض

### 👤 المتحدث الأول: هندسة المقابس وبروتوكول التأطير (Sockets & Framing)
* **الوقت المقترح:** 3 - 4 دقائق.
* **المحاور التي يشرحها:**
  1. **المشكلة والهدف:** الحاجة لنظام اتصالات محلي آمن يعمل 100% On-Premise في الشبكات المحلية LAN/Wi-Fi دون الاعتماد على الإنترنت السحابي.
  2. **دورة حياة مقابس TCP:** شرح خطوات `Socket() -> Bind() -> Listen() -> AcceptAsync()` في الخادم و `ConnectAsync() -> SendAsync() -> ReceiveAsync()` في العميل.
  3. **بروتوكول التأطير الثنائي (12-Byte FrameHeader):** شرح حقول الترويسة، الـ Magic Byte `0x53` ('S')، وترتيب بايتات الشبكة Big-Endian.
  4. **حل مشكلة تجزئة ودمج دفق TCP (Fragmentation & Merging):** شرح آلة الحالة `FrameStreamParser` وكيف تقتطع الحزم بدقة.
  5. **خدمة الاكتشاف التلقائي للشبكة (UDP Broadcast Auto-Discovery):** شرح آلية البث على المنفذ `8887` لاكتشاف IP الخادم تلقائياً.
* **الملفات المرجعية:** `FrameHeader.cs`, `FrameStreamParser.cs`, `ServerDiscovery.cs`.

---

### 👤 المتحدث الثاني: بروتوكولات الأمان والمصادقة وإدارة الجلسات (Security & Auth)
* **الوقت المقترح:** 3 - 4 دقائق.
* **المحاور التي يشرحها:**
  1. **محرك تجزئة كلمات المرور الآمن (PBKDF2/SHA-256):** شرح استخدام الـ `128-bit Salt` العشوائي المشفر، و 100,000 دورة تكرار، والمقارنة الزمنية الثابتة `FixedTimeEquals` لمنع هجمات التوقيت (Timing Attacks) وجداول قوس قزح (Rainbow Tables).
  2. **إدارة رموز الجلسات (JWT Tokens RFC 7519):** شرح توقيع التوكنات بـ HMAC-SHA256 والتحقق الفوري من الصلاحية دون استعلام قاعدة البيانات في كل حزمة.
  3. **الأمان ومكافحة حقن البيانات (Input Sanitization & Injection Defense):** حماية قاعدة البيانات عبر Parameterized SQL Queries، وحماية الشبكة بضبط سقف الحزم على 10MB لمنع هجمات حجب الخدمة (DoS).
  4. **سجلات تدقيق الأمان المؤسسي (Audit Trail):** تسجيل أحداث المصادقة وعناوين IP في جدول `SERVER_AUDIT_LOGS` وفق معايير ISO 27001.
* **الملفات المرجعية:** `CryptoEngine.cs`, `JwtTokenEngine.cs`, `UserRepository.cs`, `AuditLogRepository.cs`.

---

### 👤 المتحدث الثالث: خادم TCP، التزامن، والمزامنة المركزية (Server & Sync Engine)
* **الوقت المقترح:** 3 - 4 دقائق.
* **المحاور التي يشرحها:**
  1. **محرك خادم المقابس (TcpSocketServer Port 8888):** شرح معالجة الاتصالات المتزامنة بنمط غير حاجب عبر منافذ إكمال الإدخال والإخراج (`IOCP`)، وتطبيق نمط الحواجز المقسمة `Bulkhead Pattern` لعزل استثناءات العملاء.
  2. **قاعدة البيانات المركزية (SQLite 3NF Schema & WAL Mode):** شرح الجداول السبعة، مفاتيح الربط `Foreign Keys`، تشغيل `WAL Mode` للقراءة والكتابة المتزامنة بدون أقفال، والفهارس الذكية.
  3. **محرك المزامنة التلغرامي وطوابير الانتظار (Offline Queue):** شرح دورة حياة الرسائل وحالات التسليم (✓ / ✓✓ / ✓✓ زرقاء) ودفع الرسائل المعلقة فور عودة اتصال المستلم.
  4. **كشف انقطاع الاتصال (Half-Open Sockets):** آلية فك المقابس المعلقة عبر فحص `ReadAsync() == 0` وبروتوكول نبضات القلب دورياً (`Heartbeat`).
* **الملفات المرجعية:** `TcpSocketServer.cs`, `DatabaseManager.cs`, `MessageRepository.cs`, `SessionManager.cs`.

---

### 👤 المتحدث الرابع: مكرر وسائط UDP، إدارة الذاكرة، والبث الهجين (UDP Relay & Media)
* **الوقت المقترح:** 3 - 4 دقائق.
* **المحاور التي يشرحها:**
  1. **مقارنة TCP مع UDP عملياً:** متى ولماذا نستخدم UDP للوسائط الحية والاكتشاف التلقائي.
  2. **مكرر تدفقات الوسائط اللحظي (UdpMediaRelay Port 8889):** شرح كيفية قيام الخادم بتمرير حزم الصوت والفيديو بين أطراف المكالمة بأقل تأخير زمني.
  3. **محرك الصوت عالي النقاء HD وحماية الذاكرة (AudioEngine):** شرح معيار 16kHz HD Voice ونظام المخازن الدائرية الثابتة `Zero-Allocation Pre-Pinned Ring Buffers` والقضاء التام على خطأ `0xc0000005`.
  4. **محرك الفيديو والمكالمات (VideoEngine & PiP):** التقاط الكاميرا بمكتبة DirectShow، إطفاء ضوء الكاميرا الحقيقي (LED) عند الكتم، نافذة المعاينة الذاتية (PiP)، والبث الهجين (UDP+TCP) مع إلغاء التكرار اللحظي عبر مفتاح ثنائي 64-بت.
* **الملفات المرجعية:** `AudioEngine.cs`, `VideoEngine.cs`, `UdpMediaRelay.cs`, `MediaStreamService.cs`.

---

# 3. سيناريو العرض العملي الحي خطوة بخطوة (Live Socket Demo Playbook)

### 🎬 خطوات العرض أمام أستاذ مقرر خادم وعميل:

1. **التهيئة (قبل الدخول):**
   * توصيل جهازين (لابتوبين) على نفس شبكة الواي فاي أو نقطة اتصال الهاتف (Hotspot).
2. **إثبات الاكتشاف التلقائي عبر برودكاست UDP المنفذ 8887 (المتحدث الأول):**
   * تشغيل `SyncPulse.Server` على لابتوب (أ).
   * تشغيل `SyncPulse.Client` على لابتوب (ب)، وإظهار التقاط IP الخادم فورياً عبر UDP Broadcast دون كتابة عنوان IP يدوياً.
3. **إثبات مقبس TCP والمصادقة وتوكن JWT (المتحدث الثاني):**
   * تسجيل حسابين جديدين وتسجيل الدخول، واستعراض توليد الـ Salt والـ JWT وظهور المستخدم في لوحة الخادم.
4. **إثبات التأطير وحالات التسليم والمزامنة من طابور الانتظار (المتحدث الثالث):**
   * إرسال رسائل نصية واستعراض علامات الصح (✓ أُرسلت / ✓✓ استُلمت / ✓✓ قُرئت).
   * إغلاق أحد العملاء، إرسال رسالة إليه، ثم إعادة فتحه لإظهار **وصول الرسائل المعلقة من الـ Offline Queue فورياً**.
5. **إثبات تدفقات وسائط UDP Relay وجودة الصوت والكاميرا (المتحدث الرابع):**
   * إرسال صورة وملف صوتي واستعراض معالجة الوسائط ونقلها.
   * بدء مكالمة فيديو، استعراض **المعاينة الذاتية (PiP)** ونقاء صوت 16kHz HD.
   * الضغط على زر كتم الكاميرا وإظهار **انطفاء ضوء الكاميرا الحقيقي (LED)** على اللابتوب.
   * إنهاء المكالمة وإظهار **بقاء التطبيق مفتوحاً دون إغلاق**.

---

# 4. بنك الأسئلة التقنية العميقة لمدرس مقرر خادم وعميل

### المحور الأول: مقابس TCP وهندسة التأطير (TCP Sockets & Framing)

#### س1: اشرح بالتفصيل دورة حياة مقبس TCP في كل من الخادم والعميل؟
> **الإجابة النموذجية (المتحدث الأول):**
> "*في الخادم:*
> 1. يتم إنشاء المقبس وتحديد عائلة العناوين (`AddressFamily.InterNetwork`) ونوع المقبس (`SocketType.Stream`).
> 2. ربطه بعنوان IP ومنفذ عبر دالة `Bind(IPEndPoint)`.
> 3. وضعه في حالة الاستماع عبر `Listen(backlog)` لتحديد سعة طابور الاتصالات المعلقة.
> 4. استدعاء `AcceptAsync()` لقبول الاتصال وإنشاء مقبس مخصص لكل عميل متصل.
> 
> *في العميل:*
> 1. إنشاء المقبس واستدعاء `ConnectAsync()` لبدء مصافحة TCP الثلاثية (3-Way Handshake: SYN -> SYN-ACK -> ACK).
> 2. بعد اكتمال الاتصال، يبدأ تبادل البيانات عبر `SendAsync()` و `ReceiveAsync()`، وعند الانتهاء يتم استدعاء `Shutdown()` ثم `Close()` لتحرير المقبس."

#### س2: ما هي مشكلة تجزئة ودمج حزم TCP (TCP Stream Fragmentation / Merging)، وكيف حللتموها برمجياً؟
> **الإجابة النموذجية (المتحدث الأول):**
> "بروتوكول TCP هو بروتوكول تدفق بايتات مستمر (**Byte Stream**) وليس بروتوكول حزم مستقلة (**Message-Oriented** مثل UDP). وبسبب خوارزمية Nagle وتحسين تدفق الشبكة، قد تدمج رسالتان في قراءة واحدة (`Read`)، أو تنقسم الرسالة الكبيرة على دفعات بحسب حجم الـ MTU.
> قمنا بحل هذه المشكلة بتطبيق تقنية **بادئة الطول (Length-Prefix Framing)**؛ حيث صممنا ترويسة ثابتة بحجم **12 بايت** تحتوي على طول الحمولة `PayloadLength` ونوع الحزمة `PacketType` بترتيب Big-Endian. في المحلل `FrameStreamParser`، قمنا ببناء آلة حالة تجمع البايتات ولا تقطع الحزمة إلا بعد وصول كامل البايتات المحددة في الترويسة."

#### س3: لماذا فرضتم ترتيب بايتات الشبكة (Big-Endian Network Byte Order) في ترويسة الحزم؟
> **الإجابة النموذجية (المتحدث الأول):**
> "لأن المعالجات تختلف في طريقة تخزين الأعداد الثنائية في الذاكرة: معالجات x86/x64 تتبع نظام **Little-Endian** (البايت الأقل أهمية أولاً)، بينما معالجات ARM ومعيار الإنترنت القياسي (RFC 1700) تتبع نظام **Big-Endian**.
> إذا أرسل عميل x86 رقماً مثل `2048` كـ Little-Endian مباشرة وقرأه سيرفر أو راوتر بنظام Big-Endian، فسيقرأ الرقم بقيمة خاطئة تماماً وينهار تأطير الحزمة. لذلك استخدمنا `BinaryPrimitives.WriteInt32BigEndian` لتوحيد الترتيب وفق معيار الشبكة القياسي."

---

### المحور الثاني: نماذج التزامن وإدارة خيوط الخادم (Concurrency & Multi-Threading)

#### س4: ما هو نموذج التزامن المعتمد في الخادم؟ ولماذا لم تستخدموا نموذج خيط لكل عميل (Thread-per-Client)؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "اعتمدنا نموذج **البرمجة غير الحاجبة القائمة على الأحداث ومنافذ إكمال الإدخال والإخراج (Asynchronous I/O Completion Ports - IOCP)** عبر `async/await`.
> نموذج (Thread-per-Client) غير قابل للتوسع (Not Scalable)؛ لأن حجز خيط حقيقي لكل عميل يستهلك 1MB من الـ Stack Memory لكل خيط، ويسبب عبئاً هائلاً على المعالج في تبديل السياق (Context Switching Overhead) ويؤدي لمشكلة اختناق الخيوط (Thread Starvation). باستخدام `IOCP`، يتحرر الخيط فور بدء انتظار القراءة الشبكية، ويستيقظ خيط من الـ `ThreadPool` فقط عند وصول بايتات حقيقية من بطاقة الشبكة."

#### س5: كيف تم تطبيق نمط الحواجز المعزولة (Bulkhead Pattern) لحماية الخادم من انهيار جلسات العملاء؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "كل جلسة عميل `ClientSession` تُدار داخل مهمة غير متزامنة معزولة تماماً ومحاطة بكتلة `try-catch-finally`. فإذا انفصل اتصال عميل فجأة، أو أرسل حزمة مشوهة، أو رمى استثناء شبكي، يتم التقاط الخطأ محلياً وإغلاق مقبس هذا العميل فقط وتحرير موارده، دون أن يتأثر الخادم المركزي أو بقية العملاء المتصلين إطلاقاً."

---

### المحور الثالث: بروتوكولات النقل والمقارنة بين TCP و UDP

#### س6: متى استخدمتم TCP ومتى استخدمتم UDP في مشروعكم؟ وما هو الأساس العلمي لهذا الاختيار؟
> **الإجابة النموذجية (المتحدث الرابع):**
> 1. **بروتوكول TCP (Port 8888):** استخدمناه لعمليات **المصادقة، الرسائل النصية، نقل الملفات، وإشارات المكالمات**. الأساس العلمي: هذه البيانات تتطلب **موثوقية تامة (Reliability)** وضمان التسليم بالترتيب الصحيح بدون أي فقدان.
> 2. **بروتوكول UDP (Port 8887 للاكتشاف & Port 8889 للوسائط):** استخدمناه لـ **الاكتشاف التلقائي (Broadcast)** و **بث الصوت والفيديو اللحظي**. الأساس العلمي: الوسائط الحية لا تتحمل تأخير إعادة الإرسال (Retransmission Delay) عند سقوط حزمة، بل تتطلب **أقل زمن تأخير (Low Latency / Real-Time Delivery)**."

#### س7: كيف يعمل مكرر تدفقات الوسائط (UDP Media Relay) على الخادم؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "عند بدء المكالمة، يسجل الخادم في جدول جلسات الوسائط زوج الاتصال `(CallerEndPoint <-> ReceiverEndPoint)`.
> يستمع الخادم على منفذ UDP `8889`؛ وفور وصول أي حزمة وسائط (صوت أو فيديو) من الطرف (أ)، يقوم الخادم بقراءة عنوان المرسل وإعادة توجيه الحزمة الخام فوراً بدون أي فك تشفير إلى عنوان ومنفذ الطرف (ب). هذا يضمن سرعة تمرير تتجاوز 60 إطاراً في الثانية دون إجهاد معالج الخادم."

---

### المحور الرابع: كشف انقطاع الاتصال وإدارة الجلسات (State & Heartbeat)

#### س8: كيف يتعامل الخادم مع انقطاع العميل المفاجئ (Half-Open Sockets / Ghost Connections)؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "إذا انقطع سلك الشبكة أو أُغلق الواي فاي، لا يتم إرسال حزمة `FIN` لمقبس الخادم، فيظل المقبس مفتوحاً كـ Ghost Connection.
> قمنا بحل هذه المشكلة بطريقتين:
> 1. فحص قيمة إرجاع دالة القراءة `ReadAsync() == 0` التي تشير لانتهاء الدفق وإغلاق المقبس فورياً.
> 2. تطبيق بروتوكول نبضات القلب `Heartbeat`: يرسل العميل حزمة نبضة دورية كل 30 ثانية؛ وإذا انقضت 60 ثانية دون أي نشاط، يقوم مؤقت الخادم بإغلاق المقبس وتحرير الجلسة تلقائياً."

#### س9: كيف حميتم القوائم المشتركة للجلسات المتصلة من تضارب الخيوط (Race Conditions)؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "استخدمنا مجموعات البيانات الآمنة لخيوط المعالجة المتزامنة **`ConcurrentDictionary<int, ClientSession>`** لتخزين وإدارة الجلسات، واستخدام أقفال المزامنة `lock (SyncLock)` عند تعديل حالات الاتصال أو إرسال الحزم لضمان عدم حدوث Deadlocks أو Race Conditions."

---

### المحور الخامس: إدارة الذاكرة والأداء والأمان (Memory Safety, SQL & Crypto)

#### س10: ما هو سبب خطأ الذاكرة `0xc0000005 (STATUS_ACCESS_VIOLATION)` وكيف تم حله في محرك الصوت؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "خطأ `0xc0000005` هو خطأ انتهاك وصول الذاكرة (Access Violation) في نواة ويندوز. كان يحدث عند استخدام مؤشرات `WAVEHDR` عبر WinMM P/Invoke وتحرير الذاكرة أو تحرير تثبيت المقبض (`GCHandle.Free`) في خيط خلفي بينما بطاقة الصوت وقناة الـ DMA لا تزالان تقرآن الذاكرة المخصصة للمقطع الصوتي.
> قمنا بالقضاء على المشكلة نهائياً بابتكار **هندسة المخازن الدائرية الثابتة (Zero-Allocation Pre-Pinned Ring Buffers)**؛ حيث يتم حجز وتثبيت 8 مخازن بحجم ثابت في الذاكرة عبر `GCHandleType.Pinned` مرة واحدة فقط عند بدء المكالمة، وعند تشغيل أي مقطع يتم نسخ البايتات فقط عبر `Buffer.BlockCopy` داخل المخزن الدائري التالي دون أي حجز أو تحرير ديناميكي أثناء المكالمة، مما منع الخطأ بنسبة 100% وخفض استهلاك المعالج إلى الصفر."

#### س11: كيف يحمي النظام قاعدة البيانات من هجمات حقن SQL (SQL Injection)؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "بالاعتماد الحصري على **الاستعلامات المعلمنة (Parameterized Queries)** في محرك SQLite وحظر دمج النصوص (`String Concatenation`). يتم تمرير مدخلات المستخدم كمعاملات منفصلة في شجرة تنفيذ الاستعلام (Query Execution Tree)، مما يستحيل معه معاملة أي نص كأمر SQL تنفيذي مهما احتوى على علامات اقتباس أو شفرات خبيثة."

#### س12: لماذا استخدمتم خوارزمية PBKDF2/SHA-256 مع Salt بدلاً من خوارزميات مثل MD5 أو SHA-1؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "دوال التجزئة العادية مثل SHA-256 و MD5 صممت لتكون سريعة جداً لمعالجة الملفات، مما يجعلها نقطة ضعف خطيرة أمام هجمات التخمين باستخدام كروت الشاشة (GPU Brute-Force Cracking).
> خوارزمية **PBKDF2 مع 100,000 دورة تكرار** تُعرف بـ (Key Stretching Algorithm) وتجعل عملية حساب التجزئة الواحدة تستهلك وقتاً معالجياً مدروساً، ومع إضافة الـ `128-bit Cryptographic Salt` العشوائي لكل مستخدم، تصبح هجمات الجداول المسبقة (Rainbow Tables) مستحيلة رياضياً."

---

### المحور السادس: القرارات المعمارية والبدائل (Architectural Decisions)

#### س13: لماذا بنيتم بروتوكولكم الخاص على المقابس الخام (Raw Sockets) ولم تستخدموا SignalR أو WebRTC الجاهزة؟
> **الإجابة النموذجية (يشترك فيها الفريق):**
> "1. **الهدف التعليمي والتطبيقي لمقرر خادم وعميل:** بناء النظام على مستوى الـ Sockets يثبت الفهم الحقيقي والعميق لبروتوكولات النقل، تأطير الحزم، معالجة الـ Endianness، وإدارة الخيوط.
> 2. **العمل في الشبكات المحلية المعزولة (Offline LAN/WLAN):** مكتبات WebRTC تتطلب خوادم STUN/TURN خارجية وتفشل بدون إنترنت، بينما نظامنا يعمل فورياً داخل شبكة الواي فاي المحلية بفضل مكرر UDP Media Relay المدمج.
> 3. **الكفاءة العالية:** البروتوكول الثنائي ذو الـ 12 بايت يزيل كامل العبء الزائد (Overhead) لبروتوكولات الويب وHTTP Handshakes."

---

# 5. نصائح وإرشادات ذهبية لنيل الدرجة الكاملة

1. **الاستناد إلى الشفرة البرمجية المباشرة:** إذا سأل أستاذ المقرر عن أي جزئية، اذكر له فوراً اسم الكلاس والملف (مثال: *"هذه الجزئية تدار في `FrameStreamParser.cs` عبر دالة `ParseIncomingBytes`"*).
2. **المصطلحات الأكاديمية الدقيقة:** استخدم مصطلحات المقرر دائماً:
   - *Socket, Bind, Listen, Accept, Non-blocking I/O, IOCP, Big-Endian, Packet Framing, UDP Relay, WAL Mode, PBKDF2, Bulkhead Pattern.*
3. **التكامل والتناغم بين الـ 4 متحدثين:** التزام كل متحدث بمحوره دون مقاطعة، مع دعم الإجابة باختصار عند الحاجة.

---
**وفقكم الله وحقق لكم أعلى الدرجات ومراتب الشرف والامتياز في مقرر برمجة خادم وعميل! 🎓🌟🚀**
