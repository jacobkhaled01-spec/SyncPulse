# ميثاق وقواعد العمل الهندسية للمطورين والوكلاء البرمجيين (AGENTS.md)
### لنظام المراسلة والاتصالات المتقدم (SecureTalk / SyncPulse)
### وفق المعايير القياسية الدولية (ISO/IEC/IEEE 12207, ISO 25010, & Clean Architecture)

---

## 1. المبادئ الهندسية الأساسية الحاكمة (Core Engineering Principles)

1. **الالتزام الصارم بالعقد الموحد (Contract-First Architecture):**
   - يُمنع منعاً باتاً تعديل ترويسة الحزم (`12-Byte FrameHeader`) أو أنواع الحزم (`PacketType`) في `SyncPulse.Core` بشكل أحادي.
   - أي تعديل على النواة المشتركة يجب أن يتم عبر اتفاق مسبق و Pull Request منفصل.

2. **الفصل المادي التام للمسؤوليات (Physical Workspace Isolation):**
   - **المطور (أ) / وكيل الخادم:** يعمل حصراً داخل مشروع `SyncPulse.Server/` على فرع `feature/dev-A-server`.
   - **المطور (ب) / وكيل العميل:** يعمل حصراً داخل مشروع `SyncPulse.Client/` على فرع `feature/dev-B-client`.
   - يُحظر على أي مطور التعديل في مجلدات الطرف الآخر لتجنب أي تعارض في Git بنسبة 100%.

3. **معمارية عدم الانهيار (Zero-Crash & Fault-Tolerance Policy):**
   - **حظر العمليات الحاجبة:** يُمنع استخدام `.Result` أو `.Wait()` على أي عملية شبكية أو استعلام قاعدة بيانات؛ يجب استخدام `async/await` حصراً.
   - **عزل الاستثناءات (Bulkhead Pattern):** كل جلسة عميل على الخادم يجب أن تكون محاطة بـ `try-catch-finally` معزول تماماً، بحيث لا يؤدي انقطاع أو خطأ أي عميل إلى انهيار الخادم أو التأثير على المتصلين الآخرين.
   - **تحرير الموارد (Deterministic Resource Disposal):** تطبيق نمط `IDisposable` واستخدام `using` على كافة المقابس والملفات ومقابض قواعد البيانات لمنع تسرب الذاكرة (Memory Leaks).

4. **معايير الأمان والخصوصية (Security by Design):**
   - يُمنع تخزين كلمات المرور كنص صريح؛ يجب استخدام `CryptoEngine.HashPassword` مع الـ `Salt` الفريد عبر خوارزمية PBKDF2/SHA-256.
   - يُمنع دمج نصوص الاستعلامات مباشرة؛ يجب استخدام Parameterized Queries في SQLite لمنع حقن SQL.
   - التحقق الصارم من الـ `Magic Byte (0x53)` والحد الأقصى لحجم الحزمة ($10\text{ MB}$) قبل معالجة أي بيانات من الشبكة.

---

## 2. بروتوكول إدارة النسخ والفروع (Git & GitHub Workflow)

1. **اتفاقية رسائل الالتزام (Conventional Commits):**
   - `feat:` لإضافة ميزة جديدة.
   - `fix:` لإصلاح خطأ برمجي.
   - `refactor:` لإعادة ترتيب الكود وتحسينه دون تغيير السلوك.
   - `test:` لإضافة اختبارات جديدة.
   - `docs:` لتحديث الوثائق والتعليقات.

2. **دورة حياة الدمج (Pull Request Protocol):**
   - لا يتم الدمج المباشر في `main` إطلاقاً.
   - يتم رفع التغييرات من فروع الميزات (`feature/*`) إلى فرع `develop` عبر PR بعد التأكد من اجتياز البناء والاختبارات (`SyncPulse.Tests`).

---

## 3. محددات وثوابت الشبكة والبروتوكول (Network Constants & Specifications)

| الخاصية | القيمة المعتمدة | التوصيف |
| :--- | :---: | :--- |
| **Magic Byte** | `0x53` (`'S'`) | بايت التحقق الفوري من هوية البروتوكول. |
| **Protocol Version** | `0x01` | رقم إصدار بروتوكول التأطير. |
| **Header Length** | `12 Bytes` | الحجم الثابت لترويسة التأطير (Big-Endian). |
| **Max Payload Size** | `10 MB` | الحد الأقصى لحمولة الحزمة الواحدة. |
| **TCP Port** | `8888` | المنفذ المركزي للتحكم والمصادقة والرسائل وإشارات المكالمات. |
| **UDP Media Relay Port** | `8889` | منفذ إعادة توجيه وبث تدفقات الصوت والصورة. |
| **UDP Discovery Port** | `8887` | منفذ برودكاست الاكتشاف التلقائي على شبكات الواي فاي. |

---

## 4. نموذج المراسلة والمكالمات الفردية المباشرة (1-to-1 Architecture Rules)

1. **المراسلة الفردية (1-to-1 Messaging):**
   - الاتصال والمراسلة مباشرة وفردية حصراً بين طرفين (User A $\leftrightarrow$ User B).
   - تخزين الرسائل في الخادم بحالة `Sent = 0`، وتحديثها إلى `Delivered = 1` (علامتي صح ✓✓) فور استلام العميل المستهدف.
   - في حال عدم اتصال المستلم، يحتفظ الخادم بالرسالة في طابور الانتظار (Offline Queue) ويدفعها تلقائياً عند اتصاله.

2. **المكالمات الفردية (1-to-1 Calls):**
   - الإشارات (Offer, Ringing, Accept, Reject, End) تدار عبر TCP.
   - تدفقات الوسائط المباشرة تمرر عبر الخادم بواسطة UDP Relay على المنفذ `8889`.
   - يتم تسجيل مدة المكالمة بالثواني وحالة الإنهاء في جدول `CALL_RECORDS`.

3. **البحث وجهات الاتصال (Contacts & Discovery):**
   - دعم البحث الفوري باسم المستخدم (`@username`) وإمكانية المراسلة المباشرة أو الإضافة لجهات الاتصال.
