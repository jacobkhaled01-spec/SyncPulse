# 🎓 دليل المناقشة والشرح الهندسي الشامل لمشروع SyncPulse (SecureTalk)
### المرجع المتكامل للطلاب والمهندسين للاستعداد لمناقشة التخرج أمام لجنة الإشراف
### مخصص لفريق عمل مكون من (4 متحدثين / 4 مهندسين)
### وفق المعايير القياسية الدولية (ISO/IEC/IEEE 12207, ISO 25010, & APA 7th Edition)

---

## 📌 الفهرس العام للدليل

1. [نبذة وهوية المشروع وأهدافه الهندسية](#1-نبذة-وهوية-المشروع-وأهدافه-الهندسية)
2. [المعمارية العامة وتوزيع الطبقات (System Architecture)](#2-المعمارية-العامة-وتوزيع-الطبقات)
3. [شرح العمليات الأساسية مع الأكواد النموذجية المركزة (Core Code Snippets)](#3-شرح-العمليات-الأساسية-مع-الأكواد-النموذجية-المركزة)
   - [3.1 بروتوكول التأطير الشبكي وحل تجزئة TCP](#31-بروتوكول-التأطير-الشبكي-وحل-تجزئة-tcp-framing)
   - [3.2 الأمان والمصادقة وتشفير كلمات المرور والـ JWT](#32-الأمان-والمصادقة-وتشفير-كلمات-المرور-والـ-jwt)
   - [3.3 الاكتشاف التلقائي على شبكات الواي فاي (Auto-Discovery)](#33-الاكتشاف-التلقائي-على-شبكات-الواي-فاي-auto-discovery)
   - [3.4 دورة حياة الرسائل والمزامنة وحالات التسليم (✓ / ✓✓)](#34-دورة-حياة-الرسائل-والمزامنة-وحالات-التسليم)
   - [3.5 إشارات ومسار المكالمات الصوتية والمرئية (1-to-1 Calls)](#35-إشارات-ومسار-المكالمات-الصوتية-والمرئية)
   - [3.6 محرك الصوت عالي النقاء HD ومنع أخطاء الذاكرة (Ring Buffers)](#36-محرك-الصوت-عالي-النقاء-hd-ومنع-أخطاء-الذاكرة)
   - [3.7 عرض الوسائط المتعددة بنمط تليجرام (Rich Media Rendering)](#37-عرض-الوسائط-المتعددة-بنمط-تليجرام)
4. [مصفوفة توزيع الأدوار أثناء المناقشة (خطة الـ 4 متحدثين)](#4-مصفوفة-توزيع-الأدوار-أثناء-المناقشة-خطة-الـ-4-متحدثين)
5. [بنك الأسئلة المتوقعة الشامل وإجاباتها النموذجية مع المشرف (Defense Q&A Bank)](#5-بنك-الأسئلة-المتوقعة-الشامل-وإجاباتها-النموذجية)
   - [أ. أسئلة بروتوكولات الشبكة وهندسة المقابس (Networking & Sockets)](#أ-أسئلة-بروتوكولات-الشبكة-وهندسة-المقابس)
   - [ب. أسئلة الأمن السيبراني والمصادقة (Security & Cryptography)](#ب-أسئلة-الأمن-السيبراني-والمصادقة)
   - [ج. أسئلة الخادم وقواعد البيانات والمزامنة (Server, DB & Sync)](#ج-أسئلة-الخادم-وقواعد-البيانات-والمزامنة)
   - [د. أسئلة العميل والصوت والفيديو والذاكرة (Client, Media & Ring Buffers)](#د-أسئلة-العميل-والصوت-والفيديو-والذاكرة)
   - [هـ. أسئلة المقارنة والمزايا التنافسية (Why Not WebRTC / SignalR?)](#هـ-أسئلة-المقارنة-والمزايا-التنافسية)
6. [نصائح ذهبية وسيناريو العرض الحي (Live Demo Playbook)](#6-نصائح-ذهبية-وسيناريو-العرض-الحي)

---

# 1. نبذة وهوية المشروع وأهدافه الهندسية

* **اسم المشروع:** **SyncPulse** (الاسم الهندسي: **SecureTalk**).
* **طبيعة النظام:** نظام مراسلة فورية ومكالمات صوتية ومرئية فائقة الأمان والسرعة للشبكات المحلية واللاسلكية (**Enterprise Local Instant Messaging & Calling System**).
* **المشكلة التي يعالجها:**
  1. **الاعتماد على الإنترنت السحابي:** التطبيقات التجارية (WhatsApp, Telegram) تتطلب إنترنت خارجي، مما يعطل الاتصال في المنشآت عند انقطاع الإنترنت أو في الشبكات المعزولة أمنياً (Air-Gapped / Intranet).
  2. **انعدام الخصوصية والسيادة الرقمية:** تسريب البيانات للمزودين الخارجيين.
  3. **بطء التراسل في الشبكات المغلقة:** استهلاك حزم الإنترنت بينما الأجهزة متواجدة على نفس شبكة الواي فاي/الإيثرنت.
* **الحل المبتكر في المشروع:**
  - بناء معمارية مركزية محلية بالكامل بنمط **Client-Server** باستخدام **مكتبات المقابس الأصلية المباشرة (Raw TCP/UDP Sockets)** دون الاعتماد على مكتبات جاهزة عالية المستوى مثل SignalR أو WebRTC، مع بروتوكول تأطير ثنائي مخصص يحاكي بروتوكول **Telegram MTProto**.

---

# 2. المعمارية العامة وتوزيع الطبقات

النظام مقسم إلى 3 مشاريع مستقلة تطبق مبادئ **Clean Architecture**:

```text
+-------------------------------------------------------------------------+
|                       SyncPulse.Core (Shared Kernel)                    |
|  - 12-Byte FrameHeader   - Binary Protocol      - Packet DTOs & Enums   |
|  - CryptoEngine (PBKDF2) - JwtTokenEngine (JWT) - ServerDiscovery UDP  |
+-------------------------------------------------------------------------+
                                    ▲
                                    │ (References)
         ┌──────────────────────────┴──────────────────────────┐
         │                                                     │
+------------------------------------+  +------------------------------------+
|     SyncPulse.Server (Backend)     |  |      SyncPulse.Client (Frontend)   |
|  - TcpSocketServer (Port 8888)     |  |  - WPF MVVM Pattern (Blue/White)   |
|  - UdpMediaRelay (Port 8889)       |  |  - AudioEngine (16kHz Ring Buffers)|
|  - SQLite 3NF Database & WAL Mode  |  |  - VideoEngine (AForge Camera)     |
|  - Offline Message Queue           |  |  - Telegram Inline Media Views     |
|  - WPF SaaS Admin Dashboard        |  |  - Hybrid UDP+TCP Media Streaming  |
+------------------------------------+  +------------------------------------+
```

---

# 3. شرح العمليات الأساسية مع الأكواد النموذجية المركزة

### 3.1 بروتوكول التأطير الشبكي وحل تجزئة TCP (Framing)
* **المشكلة الهندسية:** بروتوكول TCP هو بروتوكول دفق بايتات مستمر (**Byte Stream**) وليس حزم منفصلة. قد تصل رسالتان مدمجتين في قراءة واحدة (**Packet Merging**)، أو تصل الرسالة الواحدة مجزأة على عدة دفعات (**Packet Fragmentation**).
* **الحل البرمجي:** وضع ترويسة ثابتة بحجم **12 بايت** في بداية كل حزمة، تتبع ترتيب بايتات الشبكة **Big-Endian**:

```text
+----------------+----------------+----------------+----------------+
| Magic (0x53)   | Version (0x01) | PacketType (16-bit Short)       |
+----------------+----------------+----------------+----------------+
|                   PayloadLength (32-bit Int)                      |
+----------------+----------------+----------------+----------------+
|                   SequenceNumber (32-bit Int)                     |
+----------------+----------------+----------------+----------------+
```

* **الكود المركز لإنشاء الترويسة وقراءتها (`FrameHeader.cs`):**
```csharp
// كتابة الترويسة بترتيب الشبكة Big-Endian
public byte[] ToBytes()
{
    byte[] buffer = new byte[12];
    buffer[0] = 0x53; // Magic Byte 'S'
    buffer[1] = 0x01; // Version
    BinaryPrimitives.WriteInt16BigEndian(buffer.AsSpan(2, 2), (short)PacketType);
    BinaryPrimitives.WriteInt32BigEndian(buffer.AsSpan(4, 4), PayloadLength);
    BinaryPrimitives.WriteInt32BigEndian(buffer.AsSpan(8, 4), SequenceNumber);
    return buffer;
}
```

---

### 3.2 الأمان والمصادقة وتشفير كلمات المرور والـ JWT
* **تجزئة كلمات المرور (PBKDF2 with SHA-256 & Salt):**
  يُمنع تخزين كلمة المرور صراحة. نستخدم ملح عشوائي مشفر `128-bit Salt` مع 100,000 دورة تجزئة ومقارنة زمنية ثابتة `FixedTimeEquals` لمنع هجمات التوقيت (**Timing Attacks**).

```csharp
// تشفير كلمة المرور في CryptoEngine.cs
public static string HashPassword(string password, string saltBase64)
{
    byte[] salt = Convert.FromBase64String(saltBase64);
    using var pbkdf2 = new Rfc2898DeriveBytes(password, salt, 100000, HashAlgorithmName.SHA256);
    return Convert.ToBase64String(pbkdf2.GetBytes(32));
}
```

* **جلسات المصادقة بـ JWT (RFC 7519):**
  الخادم يصدر توكن JWT موقع بخوارزمية HMAC-SHA256 يرسله العميل مع كل طلب للتحقق الفوري دون الضغط على قاعدة البيانات.

---

### 3.3 الاكتشاف التلقائي على شبكات الواي فاي (Auto-Discovery)
* يقوم الخادم ببث حزمة UDP Broadcast خفيفة دورياً على المنفذ `8887`. يستمع العميل لها ليعثر على عنوان IP الخادم تلقائياً دون إدخاله يدوياً:

```csharp
// الخادم يبث إعلانه على منفذ 8887
using var udp = new UdpClient { EnableBroadcast = true };
byte[] announcement = Encoding.UTF8.GetBytes("SYNCPULSE_SERVER_ANNOUNCE:8888");
await udp.SendAsync(announcement, announcement.Length, new IPEndPoint(IPAddress.Broadcast, 8887));

// العميل يستقبل ويكتشف السيرفر فورياً
var result = await udpListener.ReceiveAsync();
string msg = Encoding.UTF8.GetString(result.Buffer);
string serverIP = result.RemoteEndPoint.Address.ToString(); // تم الاكتشاف!
```

---

### 3.4 دورة حياة الرسائل والمزامنة وحالات التسليم
1. يرسل العميل الرسالة `DirectChatMessage` بحالة `Sent` (**✓ واحدة**).
2. يحفظها الخادم في قاعدة بيانات `SQLite (WAL Mode)`.
3. إذا كان المستلم متصلاً: يمررها الخادم فوراً ويعيد إشعار تسليم `MessageDeliveredAck` للمرسل لتتحول إلى (**✓✓ رمادية**).
4. إذا فتح المستلم المحادثة: يرسل `MessageReadAck` للمرسل لتتحول إلى (**✓✓ زرقاء**).
5. إذا كان المستلم غير متصل: تظل في **Offline Queue** وتُدفع له تلقائياً فور اتصاله.

```csharp
// التحقق من حالة المستلم وتمرير الرسالة في PacketDispatcher.cs
var targetSession = _sessionManager.GetSessionByUserID(msg.ReceiverID);
if (targetSession != null && targetSession.IsConnected)
{
    await targetSession.SendPacketAsync(SyncPacket.Create(PacketType.DirectChatMessage, msg));
    _messageRepo.UpdateMessageStatus(msg.MessageID, MessageStatus.Delivered);
}
```

---

### 3.5 إشارات ومسار المكالمات الصوتية والمرئية
تتبع المكالمات معمارية **الهجينة (TCP للإشارات + Dual-Channel للوسائط)**:

```text
[Alice] --(1. Offer: TCP 8888)--> [Server] --(2. Incoming: TCP 8888)--> [Bob]
[Alice] <--(4. Accept: TCP 8888)-- [Server] <--(3. Accept: TCP 8888)--- [Bob]
====================== بدء تدفقات الوسائط المباشرة ======================
[Alice] <==== (UDP 8889 + Fallback TCP عبر السيرفر) ====> [Bob]
```

---

### 3.6 محرك الصوت عالي النقاء HD ومنع أخطاء الذاكرة (Ring Buffers)
* **ترقية HD Voice:** يعمل بتردد `16000 Hz, 16-bit Mono (1280 bytes per 40ms)`.
* **الحماية من انهيار الذاكرة `0xc0000005`:** استخدام **8 مخازن تشغيل دائرية مثبتة مسبقاً (Pre-Pinned Ring Buffers)** دون أي حجز ديناميكي للذاكرة أثناء المكالمة:

```csharp
// تشغيل الصوت في AudioEngine.cs بدون أي حجز للذاكرة
lock (_outLock)
{
    int idx = _outBufferIndex;
    _outBufferIndex = (_outBufferIndex + 1) % OutBufferCount; // المخزن الدائري

    Buffer.BlockCopy(pcmData, 0, _outBuffers[idx], 0, pcmData.Length);
    waveOutWrite(_hWaveOut, _pOutHeaders[idx], Marshal.SizeOf<WAVEHDR>());
}
```

---

### 3.7 عرض الوسائط المتعددة بنمط تليجرام
* تظهر الصور والملفات الصوتية والمستندات **على طبيعتها مباشرة داخل فقاعة الرسالة (Inline Display)** مع زوايا دائرية وأزرار تشغيل فورية:

```xaml
<!-- عرض الصورة المباشرة في MainChatView.xaml -->
<Border CornerRadius="10" ClipToBounds="True" MaxWidth="360">
    <Image Source="{Binding ImagePreviewSource}" Stretch="Uniform" MaxHeight="260" />
</Border>
```

---

# 4. مصفوفة توزيع الأدوار أثناء المناقشة (خطة الـ 4 متحدثين)

تم تقسيم وتوزيع محاور المشروع بدقة هندسية عالية بحيث ينال كل عضو من الأعضاء الأربعة **دوراً رئيسياً ومرموقاً ومتكاملاً**:

```text
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                         خريطة توزيع الأدوار الهندسية (فريق الـ 4 متحدثين)                 │
├──────────────────────────┬──────────────────────────┬────────────────────────────────────┤
│ المتحدث الأول            │ المتحدث الثاني           │ المتحدث الثالث                     │ المتحدث الرابع
│ مهندس المعمارية والشبكات │ مهندس الأمن والمصادقة    │ مهندس الخادم وقواعد البيانات       │ مهندس العميل والوسائط والميديا
│ (Lead Network Architect) │ (Cyber Security & Auth)  │ (Backend & Database Engineer)      │ (Frontend & Media Engineer)
└──────────────────────────┴──────────────────────────┴────────────────────────────────────┴────────────────────────────────────┘
```

---

### 👤 المتحدث الأول: مهندس المعمارية والشبكات وبروتوكول التأطير (Lead Network & Protocol Architect)
* **المسؤوليات والعرض التقديمي:**
  1. **افتتاح العرض:** تقديم مشكلة المشروع (الحاجة لنظام اتصالات محلي آمن ومستقل عن الإنترنت) وأهدافه الهندسية.
  2. **معمارية النظام العامة:** شرح تقسيم النظام وفق Clean Architecture (الخادم، العميل، النواة المشتركة).
  3. **بروتوكول التأطير الثنائي (12-Byte FrameHeader):** شرح تركيبة الـ 12 بايت، الـ Magic Byte `0x53` ('S')، وترتيب بايتات الشبكة Big-Endian.
  4. **حل مشكلة تجزئة ودمج دفق TCP (Fragmentation & Merging):** شرح آلة الحالة `FrameStreamParser` وكيف تضمن قراءة الحزم بدقة.
  5. **خدمة الاكتشاف التلقائي لشبكات الواي فاي (UDP Auto-Discovery):** شرح آلية البث على المنفذ `8887` والاتصال التلقائي بدون كتابة عنوان IP.
* **الملفات التي يركز عليها:** `FrameHeader.cs`, `FrameStreamParser.cs`, `ServerDiscovery.cs`, `SyncPacket.cs`.

---

### 👤 المتحدث الثاني: مهندس الأمن السيبراني والمصادقة وإدارة الجلسات (Cyber Security & Auth Engineer)
* **المسؤوليات والعرض التقديمي:**
  1. **محرك تجزئة كلمات المرور الآمن (PBKDF2/SHA-256):** شرح آلية التشفير باستخدام الـ `128-bit Salt` العشوائي المشفر، و 100,000 دورة تكرار، والمقارنة الزمنية الثابتة `FixedTimeEquals` لمنع هجمات التوقيت وجداول قوس قزح.
  2. **إدارة رموز الجلسات (JWT Tokens RFC 7519):** شرح توليد والتحقق من التوكنات الموقعة بـ HMAC-SHA256، وتجنب فحص قاعدة البيانات في كل حزمة.
  3. **الأمان ومكافحة حقن البيانات (Input Sanitization & Injection Defense):** شرح حماية قاعدة البيانات عبر Parameterized SQL Queries، وحماية الشبكة بضبط سقف الحزم على 10MB لمنع هجمات حجب الخدمة (DoS).
  4. **سجلات التدقيق والأمان المؤسسي (Audit Trail):** شرح جدول `SERVER_AUDIT_LOGS` وتسجيل أحداث المصادقة وعناوين IP وفق معايير ISO 27001.
* **الملفات التي يركز عليها:** `CryptoEngine.cs`, `JwtTokenEngine.cs`, `UserRepository.cs`, `AuditLogRepository.cs`.

---

### 👤 المتحدث الثالث: مهندس الخادم وقواعد البيانات والمزامنة (Backend & Database Engineer)
* **المسؤوليات والعرض التقديمي:**
  1. **محرك خادم المقابس (TcpSocketServer Port 8888):** شرح معالجة الاتصالات المتزامنة بنمط غير حاجب (`async/await`) وتطبيق نمط الحواجز المقسمة `Bulkhead Pattern` لعزل استثناءات العملاء.
  2. **قاعدة البيانات المركزية (SQLite 3NF Schema & WAL Mode):** شرح الجداول السبعة، مفاتيح الربط `Foreign Keys`، تشغيل `WAL Mode` للقراءة والكتابة المتزامنة بدون أقفال، والفهارس الذكية.
  3. **محرك المزامنة التلغرامي وطوابير الانتظار (Offline Queue):** شرح دورة حياة الرسائل وحالات التسليم (✓ / ✓✓ / ✓✓ زرقاء) ودفع الرسائل المعلقة فور عودة اتصال المستلم.
  4. **مكرر الوسائط المباشر (UdpMediaRelay Port 8889):** شرح كيفية قيام الخادم بتمرير حزم الصوت والفيديو بين أطراف المكالمة بأقل تأخير زمني.
* **الملفات التي يركز عليها:** `TcpSocketServer.cs`, `DatabaseManager.cs`, `MessageRepository.cs`, `UdpMediaRelay.cs`, `SessionManager.cs`.

---

### 👤 المتحدث الرابع: مهندس العميل والوسائط ومعالجة الصوت والفيديو (Frontend & Real-Time Media Engineer)
* **المسؤوليات والعرض التقديمي:**
  1. **واجهة العميل بنمط WPF MVVM:** شرح بنية العرض باللونين الأزرق والأبيض النقي، عدم تجميد الواجهة (Zero UI Freezing)، ومؤشر الكتابة اللحظي (`✍️ يكتب الآن...`).
  2. **محرك الصوت عالي النقاء HD وحماية الذاكرة (AudioEngine):** شرح معيار 16kHz HD Voice ونظام المخازن الدائرية الثابتة `Zero-Allocation Pre-Pinned Ring Buffers` والقضاء التام على خطأ `0xc0000005`.
  3. **محرك التقاط الفيديو والمعاينة الذاتية (VideoEngine & PiP):** شرح التقاط الكاميرا بمكتبة AForge، إطفاء ضوء الكاميرا الحقيقي (LED) عند الكتم، نافذة المعاينة الذاتية في الزاوية (PiP)، والبث الهجين (UDP+TCP).
  4. **عرض الوسائط المتعددة بنمط تليجرام:** شرح العرض المباشر للصور داخل الفقاعة (Inline Display)، بطاقات الصوت التفاعلية، وبطاقات المستندات الذكية مع أزرار الفتح والحفظ.
* **الملفات التي يركز عليها:** `AudioEngine.cs`, `VideoEngine.cs`, `MediaStreamService.cs`, `MessageItem.cs`, `MainChatView.xaml`, `CallWindow.xaml`.

---

# 5. بنك الأسئلة المتوقعة الشامل وإجاباتها النموذجية

### أ. أسئلة بروتوكولات الشبكة وهندسة المقابس (Networking & Sockets)

#### س1: لماذا استخدمتم كلاً من بروتوكول TCP وبروتوكول UDP معاً في النظام ولم تكتفوا بأحدهما؟
> **الإجابة النموذجية (المتحدث الأول):** 
> "استخدمنا المعمارية الهجينة وفق متطلبات كل خدمة:
> 1. **بروتوكول TCP:** استخدمناه للمصادقة والرسائل النصية ونقل الملفات وإشارات المكالمات، لأن هذه العمليات تتطلب **موثوقية تامة (Reliability)** وعدم فقدان أي بايت وضمان الترتيب الصحيح.
> 2. **بروتوكول UDP:** استخدمناه لبث الصوت والفيديو اللحظي وميزة الاكتشاف التلقائي، لأن الوسائط الحية تتطلب **أقل تأخير زمني ممكن (Low Latency)**؛ فإذا تأخرت حزمة صوتية أو سقطت، لا فائدة من إعادة طلبها لأن زمنها قد مضى."

#### س2: ما هي مشكلة تجزئة حزم TCP (TCP Streaming Fragmentation / Packet Merging) وكيف حللتموها في الكود؟
> **الإجابة النموذجية (المتحدث الأول):**
> "بروتوكول TCP يعمل كـ Stream مستمر للبايتات ولا يعرف حدود الرسائل. في حالات ضغط الشبكة قد تصل عدة رسائل مدمجة في قراءة واحدة أو تصل الرسالة الواحدة مجزأة على دفعات.
> قمنا بحل هذه المشكلة بتصميم **بروتوكول تأطير ثنائي مخصص (12-Byte FrameHeader)** يحدد طول الحمولة `PayloadLength` بدقة ونوع الحزمة `PacketType` بترتيب `Big-Endian`. في العميل والخادم، قمنا ببناء آلة حالة (`FrameStreamParser`) تقوم بتجميع البايتات وقراءة الترويسة أولاً، ثم لا تقطع الحزمة إلا بعد اكتمال كامل حجم الحمولة المحدد."

#### س3: كيف تعمل ميزة الاكتشاف التلقائي للخادم (Auto-Discovery) على شبكة الواي فاي بدون كتابة IP؟
> **الإجابة النموذجية (المتحدث الأول):**
> "عبر استخدام **UDP Broadcast على المنفذ 8887**. يقوم الخادم ببث حزمة إعلان دورية على عنوان البرودكاست `255.255.255.255` لكافة بطاقات الشبكة المتاحة. عند فتح العميل، يستمع على نفس المنفذ ليلتقط الإعلان ويستخرج منه عنوان الـ IP الفعلي للخادم ويتصل به تلقائياً."

---

### ب. أسئلة الأمن السيبراني والمصادقة (Security & Cryptography)

#### س4: لماذا استخدمتم خوارزمية PBKDF2/SHA-256 مع Salt بدلاً من خوارزميات مثل MD5 أو SHA-1؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "خوارزميات MD5 و SHA-1 تعتبر مكسورة أمنياً وعرضة لهجمات التصادم (Collisions) وهجمات جداول قوس قزح (Rainbow Tables).
> استخدمنا **PBKDF2 مع SHA-256 و 128-bit Cryptographic Salt و 100,000 دورة تكرار** وفق معيار **NIST SP 800-63B**، مما يجعل كسر كلمة المرور بالقوة الغاشمة (Brute-force) مستحيلاً عملياً."

#### س5: كيف تتم إدارة جلسات المستخدمين بعد تسجيل الدخول (Session Management)؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "يصدر الخادم رمز **JWT (JSON Web Token)** موقع رقمياً بمفتاح سري مشفر عبر خوارزمية HMAC-SHA256 وفق معيار RFC 7519. يحمل التوكن معرف المستخدم واسمه وتاريخ انتهاء الصلاحية. عند إرسال أي طلب لاحق، يتحقق الخادم من صحة التوقيع رياضياً فورياً دون الحاجة للاستعلام عن المستخدم في قاعدة البيانات، مما يوفر سرعة استجابة فائقة."

#### س6: كيف يحقق نظامكم ميزة "السيادة الرقمية والأمان المؤسسي"؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "نظامنا يعمل **100% On-Premise داخل الشبكة المحلية للمؤسسة**. لا تخرج أي رسالة أو ملف أو مقطع فيديو إلى الإنترنت أو خوادم سحابية خارجية، مما يحمي البيانات الحساسة من أي تنصت خارجي أو اختراق لسلاسل الإمداد."

---

### ج. أسئلة الخادم وقواعد البيانات والمزامنة (Server, DB & Sync)

#### س7: لماذا اخترتم قاعدة بيانات SQLite وكيف تم ضبطها للأداء العالي؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "SQLite هي قاعدة بيانات علائقية خفيفة، مدمجة، ولا تتطلب تثبيت خوادم معقدة، وهي مناسبة جداً للأنظمة المحلية المستقلة.
> لضمان الأداء الفائق والتزامن، قمنا بتفعيل **وضع WAL (Write-Ahead Logging)** عبر الأمر `PRAGMA journal_mode = WAL;` الذي يسمح بعدة قراءات متزامنة أثناء الكتابة دون قفل قاعدة البيانات، بالإضافة لتفعيل مفاتيح الربط `PRAGMA foreign_keys = ON;` وتصميم الجداول وفق **الشكل المعياري الثالث (3NF)** مع فهارس ذكية."

#### س8: كيف تعمل آلية المزامنة وطابور الرسائل المعلقة (Offline Queue) عند انقطاع اتصال المستخدم؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "عند إرسال رسالة لمستخدم غير متصل، يتم حفظها في جدول `MESSAGES` بحالة `Sent = 0`. بمجرد تسجيل دخول المستلم لاحقاً، يقوم الخادم بفحص الرسائل المعلقة ويقوم بدفعها له تلقائياً، ثم يُحدث حالتها إلى `Delivered = 1` ويرسل إشعاراً للمرسل لتظهر لديه علامتا صح (**✓✓**)."

#### س9: كيف يتم حماية الخادم من الانهيار عند انقطاع أحد المتصلين (Bulkhead Pattern)؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "طبقنا نمط الحواجز المقسمة `Bulkhead Pattern`؛ فكل جلسة عميل `ClientSession` تعمل في نطاق غير متزامن محاط بـ `try-catch-finally` معزول تماماً. إذا انقطع اتصال عميل أو أرسل بيانات غير صالحة، يحرر الخادم مقبس هذا العميل بأمان دون التأثير على بقية المستخدمين المتصلين."

---

### د. أسئلة العميل والصوت والفيديو والذاكرة (Client, Media & Ring Buffers)

#### س10: ما كان سبب خطأ الذاكرة `0xc0000005 (Access Violation)` وكيف تم القضاء عليه نهائياً؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "خطأ `0xc0000005` هو خطأ وصول غير مصرح به للذاكرة في نظام ويندوز. كان يحدث عند استخدام مؤشرات `WAVEHDR` عبر WinMM P/Invoke وتحريرها ديناميكياً أثناء انشغال بطاقة الصوت بتشغيل المقطع.
> قمنا بحل المشكلة بتصميم **هندسة المخازن الدائرية الثابتة (Zero-Allocation Pre-Pinned Ring Buffers)**؛ حيث يتم حجز وتثبيت 8 مخازن في الذاكرة عبر `GCHandle.Alloc(Pinned)` مرة واحدة فقط عند بدء المكالمة وإعادة الكتابة داخلها دورياً دون أي حجز أو تحرير ديناميكي للذاكرة أثناء المكالمة."

#### س11: كيف تم ضمان إطفاء ضوء الكاميرا الحقيقي (LED) عند كتم الفيديو؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "باستخدام مكتبة `AForge.Video.DirectShow` للتحكم الفيزيائي في مقبض الكاميرا؛ فعند الضغط على كتم الكاميرا يتم إيقاف الإشارة (`_videoSource.SignalToStop()`) وتحرير المقبض فيطفئ ضوء الكاميرا الحقيقي على اللابتوب فورياً لحماية خصوصية المستخدم، وتتم إعادة تشغيله بسلاسة عند إلغاء الكتم."

#### س12: كيف يعمل نظام عرض الوسائط بنمط تليجرام في الواجهة؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "في `MainChatView.xaml`، بدلاً من إظهار المرفقات كملفات عادية، قمنا ببناء قوالب بيانات ذكية (DataTemplates):
> - للصور: فك التشفير عبر `BitmapImage` المفرزة وعرضها مباشرة داخل الفقاعة بأبعاد متناسقة وزوايا دائرية.
> - للصوت: بطاقة مشغل صوتي مدمجة مع زر تشغيل `▶` وحجم الملف.
> - للمستندات: بطاقات مخصصة بأيقونات ملونة بحسب الامتداد مع أزرار الفتح والحفظ."

---

### هـ. أسئلة المقارنة والمزايا التنافسية (Why Not WebRTC / SignalR?)

#### س13: لماذا بنيتم بروتوكولكم الخاص ولم تستخدموا SignalR أو WebRTC الجاهزة؟
> **الإجابة النموذجية (يشترك فيها الفريق):**
> "1. **التحكم الهندسي الكامل:** بناء البروتوكول على Raw Sockets منحنا فهماً عميقاً لتحكم الشبكة والتحكم في بنية الترويسة 12-Byte بدون استهلاك زائد للبيانات (Overhead).
> 2. **العمل في الشبكات المعزولة (Offline LAN/WLAN):** مكتبات WebRTC تتطلب خوادم STUN/TURN خارجية للـ NAT Traversal، بينما نظامنا مصمم ليعمل فورياً وبكفاءة قصوى داخل الشبكات المحلية بدون أي إنترنت خارجي.
> 3. **الكفاءة واستهلاك الموارد:** بروتوكولنا الثنائي مدمج وخفيف جداً ولا يستهلك ذاكرة أو معالجة مقارنة بأطر العمل الكبيرة."

---

# 6. نصائح ذهبية وسيناريو العرض الحي (Live Demo Playbook)

### 🎬 سيناريو العرض العملي الحي خطوة بخطوة أمام اللجنة:

1. **التهيئة (قبل الدخول):**
   - توصيل لابتوبين على نفس شبكة الواي فاي أو نقطة اتصال الهاتف (Hotspot).
2. **بدء العرض (المتحدث الأول):**
   - تشغيل `SyncPulse.Server` واستعراض لوحة التحكم الإدارية.
   - تشغيل `SyncPulse.Client` واستعراض **الاكتشاف التلقائي الفوري للسيرفر** دون كتابة IP.
3. **المصادقة والأمان (المتحدث الثاني):**
   - تسجيل حسابين جديدين وتسجيل الدخول، واستعراض توليد الـ Salt والـ JWT وظهور المستخدم في لوحة الخادم.
4. **المراسلة والمزامنة (المتحدث الثالث):**
   - إرسال رسائل نصية واستعراض علامات الصح (✓ أُرسلت / ✓✓ استُلمت / ✓✓ قُرئت).
   - إغلاق العميل الثاني، إرسال رسالة، ثم إعادة فتحه لإظهار **وصول الرسائل المعلقة من الـ Offline Queue فورياً**.
5. **الوسائط والمكالمات الحية (المتحدث الرابع):**
   - إرسال صورة وملف صوتي واستعراض **عرض وسائط تليجرام المباشرة**.
   - بدء مكالمة فيديو، استعراض **المعاينة الذاتية (PiP)** ونقاء صوت HD.
   - الضغط على زر كتم الكاميرا وإظهار **انطفاء ضوء الكاميرا الحقيقي (LED)** على اللابتوب.
   - إنهاء المكالمة وإظهار **بقاء التطبيق مفتوحاً دون إغلاق**.

---
**وفقكم الله وحقق لكم أعلى الدرجات ومراتب الشرف والامتياز! 🎓🌟🚀**
