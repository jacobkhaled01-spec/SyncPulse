# 🎓 دليل المناقشة والشرح الهندسي الشامل لمشروع SyncPulse (SecureTalk)
### المرجع المتكامل للطلاب والمهندسين للاستعداد لمناقشة التخرج أمام لجنة الإشراف
### مخصص لفريق عمل مكون من (4 متحدثين / 4 مهندسين)
### تركيز هندسي تقني عميق (بروتوكولات، أمن، أنظمة تشغيل، إدارة ذاكرة، وتزامن)
### وفق المعايير القياسية الدولية (ISO/IEC/IEEE 12207, ISO 25010, & APA 7th Edition)

---

## 📌 الفهرس العام للدليل

1. [نبذة وهوية المشروع وأهدافه الهندسية](#1-نبذة-وهوية-المشروع-وأهدافه-الهندسية)
2. [المعمارية العامة وتوزيع الطبقات (System Architecture)](#2-المعمارية-العامة-وتوزيع-الطبقات)
3. [شرح العمليات الأساسية مع الأكواد النموذجية المركزة (Core Code Snippets)](#3-شرح-العمليات-الأساسية-مع-الأكواد-النموذجية-المركزة)
   - [3.1 بروتوكول التأطير الشبكي وحل تجزئة TCP](#31-بروتوكول-التأطير-الشبكي-وحل-تجزئة-tcp-framing)
   - [3.2 الأمان والمصادقة وتشفير كلمات المرور والـ JWT](#32-الأمان-والمصادقة-وتشفير-كلمات-المرور-والـ-jwt)
   - [3.3 الاكتشاف التلقائي على شبكات الواي فاي (Auto-Discovery)](#33-الاكتشاف-التلقائي-على-شبكات-الواي-فاي-auto-discovery)
   - [3.4 دورة حياة الرسائل والمزامنة وحالات التسليم (✓ / ✓✓)](#34-دورة-حياة-الرسائل-والمزامنة-وحالات-التسليم)
   - [3.5 إشارات ومسار المكالمات الصوتية والمرئية (1-to-1 Calls)](#35-إشارات-ومسار-المكالمات-الصوتية-والمرئية)
   - [3.6 محرك الصوت عالي النقاء HD ومنع أخطاء الذاكرة (Ring Buffers)](#36-محرك-الصوت-عالي-النقاء-hd-ومنع-أخطاء-الذاكرة)
   - [3.7 محرك التقاط الفيديو ومعالجة الصور المباشرة (Video & Media)](#37-محرك-التقاط-الفيديو-ومعالجة-الصور-المباشرة)
4. [مصفوفة توزيع الأدوار أثناء المناقشة (خطة الـ 4 متحدثين)](#4-مصفوفة-توزيع-الأدوار-أثناء-المناقشة-خطة-الـ-4-متحدثين)
5. [بنك الأسئلة التقنية العميقة المتوقعة وإجاباتها النموذجية (Technical Defense Q&A Bank)](#5-بنك-الأسئلة-التقنية-العميقة-المتوقعة-وإجاباتها-النموذجية)
   - [أ. أسئلة بروتوكولات الشبكة وهندسة المقابس (Networking & Sockets)](#أ-أسئلة-بروتوكولات-الشبكة-وهندسة-المقابس)
   - [ب. أسئلة الأمن السيبراني وعلم التشفير (Cyber Security & Cryptography)](#ب-أسئلة-الأمن-السيبراني-وعلم-التشفير)
   - [ج. أسئلة الخادم والتزامن وقواعد البيانات (Concurrency, Server & DB Engine)](#ج-أسئلة-الخادم-والتزامن-وقواعد-البيانات)
   - [د. أسئلة أنظمة التشغيل وإدارة الذاكرة والميديا (OS, Memory, P/Invoke & Streaming)](#د-أسئلة-أنظمة-التشغيل-وإدارة-الذاكرة-والميديا)
   - [هـ. أسئلة المقارنة والقرارات المعمارية (Architectural Trade-offs)](#هـ-أسئلة-المقارنة-والقرارات-المعمارية)
6. [نصائح ذهبية وسيناريو العرض الحي (Live Demo Playbook)](#6-نصائح-ذهبية-وسيناريو-العرض-الحي)

---

# 1. نبذة وهوية المشروع وأهدافه الهندسية

* **اسم المشروع:** **SyncPulse** (الاسم الهندسي: **SecureTalk**).
* **طبيعة النظام:** نظام مراسلة فورية ومكالمات صوتية ومرئية فائقة الأمان والسرعة للشبكات المحلية واللاسلكية (**Enterprise Local Instant Messaging & Calling System**).
* **المشكلة التي يعالجها:**
  1. **الاعتماد على الإنترنت السحابي:** التطبيقات التجارية (WhatsApp, Telegram) تتطلب إنترنت خارجي، مما يعطل الاتصال في المنشآت عند انقطاع الإنترنت أو في الشبكات المعزولة أمنياً (Air-Gapped / Intranet).
  2. **انعدام الخصوصية والسيادة الرقمية:** تسريب البيانات للمزودين الخارجيين.
  3. **بطء التراسل في الشبكات المغلقة:** استهلاك حزم الإنترنت بينما الأجهزة متواجدة على نفس شبكة الواي فاي/الإيثرنت.
* **الحل المبتكر في المشروع:**
  - بناء معمارية مركزية محلية بالكامل بنمط **Client-Server** باستخدام **مكتبات المقابس الأصلية المباشرة (Raw TCP/UDP Sockets)** دون الاعتماد على مكتبات جاهزة عالية المستوى مثل SignalR أو WebRTC، مع بروتوكول تأطير ثنائي مخصص يحاكي بروتوكول **Telegram MTProto**.

---

# 2. المعمارية العامة وتوزيع الطبقات

النظام مقسم إلى 3 مشاريع مستقلة تطبق مبادئ **Clean Architecture**:

```text
+-------------------------------------------------------------------------+
|                       SyncPulse.Core (Shared Kernel)                    |
|  - 12-Byte FrameHeader   - Binary Protocol      - Packet DTOs & Enums   |
|  - CryptoEngine (PBKDF2) - JwtTokenEngine (JWT) - ServerDiscovery UDP  |
+-------------------------------------------------------------------------+
                                    ▲
                                    │ (References)
         ┌──────────────────────────┴──────────────────────────┐
         │                                                     │
+------------------------------------+  +------------------------------------+
|     SyncPulse.Server (Backend)     |  |      SyncPulse.Client (Frontend)   |
|  - TcpSocketServer (Port 8888)     |  |  - Raw Socket Async Network Layer  |
|  - UdpMediaRelay (Port 8889)       |  |  - AudioEngine (16kHz Ring Buffers)|
|  - SQLite 3NF Database & WAL Mode  |  |  - VideoEngine (AForge DirectShow) |
|  - Offline Message Queue           |  |  - Inline Image BitmapSource Engine|
|  - Multi-Client Bulkhead Isolation |  |  - Hybrid UDP+TCP Media Streaming  |
+------------------------------------+  +------------------------------------+
```

---

# 3. شرح العمليات الأساسية مع الأكواد النموذجية المركزة

### 3.1 بروتوكول التأطير الشبكي وحل تجزئة TCP (Framing)
* **المشكلة الهندسية:** بروتوكول TCP هو بروتوكول دفق بايتات مستمر (**Byte Stream**) وليس حزم منفصلة. قد تصل رسالتان مدمجتين في قراءة واحدة (**Packet Merging**)، أو تصل الرسالة الواحدة مجزأة على عدة دفعات (**Packet Fragmentation**).
* **الحل البرمجي:** وضع ترويسة ثابتة بحجم **12 بايت** في بداية كل حزمة، تتبع ترتيب بايتات الشبكة **Big-Endian**:

```text
+----------------+----------------+----------------+----------------+
| Magic (0x53)   | Version (0x01) | PacketType (16-bit Short)       |
+----------------+----------------+----------------+----------------+
|                   PayloadLength (32-bit Int)                      |
+----------------+----------------+----------------+----------------+
|                   SequenceNumber (32-bit Int)                     |
+----------------+----------------+----------------+----------------+
```

* **الكود المركز لإنشاء الترويسة وقراءتها (`FrameHeader.cs`):**
```csharp
// كتابة الترويسة بترتيب الشبكة Big-Endian
public byte[] ToBytes()
{
    byte[] buffer = new byte[12];
    buffer[0] = 0x53; // Magic Byte 'S'
    buffer[1] = 0x01; // Version
    BinaryPrimitives.WriteInt16BigEndian(buffer.AsSpan(2, 2), (short)PacketType);
    BinaryPrimitives.WriteInt32BigEndian(buffer.AsSpan(4, 4), PayloadLength);
    BinaryPrimitives.WriteInt32BigEndian(buffer.AsSpan(8, 4), SequenceNumber);
    return buffer;
}
```

---

### 3.2 الأمان والمصادقة وتشفير كلمات المرور والـ JWT
* **تجزئة كلمات المرور (PBKDF2 with SHA-256 & Salt):**
  يُمنع تخزين كلمة المرور صراحة. نستخدم ملح عشوائي مشفر `128-bit Salt` مع 100,000 دورة تجزئة ومقارنة زمنية ثابتة `FixedTimeEquals` لمنع هجمات التوقيت (**Timing Attacks**).

```csharp
// تشفير كلمة المرور في CryptoEngine.cs
public static string HashPassword(string password, string saltBase64)
{
    byte[] salt = Convert.FromBase64String(saltBase64);
    using var pbkdf2 = new Rfc2898DeriveBytes(password, salt, 100000, HashAlgorithmName.SHA256);
    return Convert.ToBase64String(pbkdf2.GetBytes(32));
}
```

* **جلسات المصادقة بـ JWT (RFC 7519):**
  الخادم يصدر توكن JWT موقع بخوارزمية HMAC-SHA256 يرسله العميل مع كل طلب للتحقق الفوري دون الضغط على قاعدة البيانات.

---

### 3.3 الاكتشاف التلقائي على شبكات الواي فاي (Auto-Discovery)
* يقوم الخادم ببث حزمة UDP Broadcast خفيفة دورياً على المنفذ `8887`. يستمع العميل لها ليعثر على عنوان IP الخادم تلقائياً دون إدخاله يدوياً:

```csharp
// الخادم يبث إعلانه على منفذ 8887
using var udp = new UdpClient { EnableBroadcast = true };
byte[] announcement = Encoding.UTF8.GetBytes("SYNCPULSE_SERVER_ANNOUNCE:8888");
await udp.SendAsync(announcement, announcement.Length, new IPEndPoint(IPAddress.Broadcast, 8887));

// العميل يستقبل ويكتشف السيرفر فورياً
var result = await udpListener.ReceiveAsync();
string msg = Encoding.UTF8.GetString(result.Buffer);
string serverIP = result.RemoteEndPoint.Address.ToString(); // تم الاكتشاف!
```

---

### 3.4 دورة حياة الرسائل والمزامنة وحالات التسليم
1. يرسل العميل الرسالة `DirectChatMessage` بحالة `Sent` (**✓ واحدة**).
2. يحفظها الخادم في قاعدة بيانات `SQLite (WAL Mode)`.
3. إذا كان المستلم متصلاً: يمررها الخادم فوراً ويعيد إشعار تسليم `MessageDeliveredAck` للمرسل لتتحول إلى (**✓✓ رمادية**).
4. إذا فتح المستلم المحادثة: يرسل `MessageReadAck` للمرسل لتتحول إلى (**✓✓ زرقاء**).
5. إذا كان المستلم غير متصل: تظل في **Offline Queue** وتُدفع له تلقائياً فور اتصاله.

```csharp
// التحقق من حالة المستلم وتمرير الرسالة في PacketDispatcher.cs
var targetSession = _sessionManager.GetSessionByUserID(msg.ReceiverID);
if (targetSession != null && targetSession.IsConnected)
{
    await targetSession.SendPacketAsync(SyncPacket.Create(PacketType.DirectChatMessage, msg));
    _messageRepo.UpdateMessageStatus(msg.MessageID, MessageStatus.Delivered);
}
```

---

### 3.5 إشارات ومسار المكالمات الصوتية والمرئية
تتبع المكالمات معمارية **الهجينة (TCP للإشارات + Dual-Channel للوسائط)**:

```text
[Alice] --(1. Offer: TCP 8888)--> [Server] --(2. Incoming: TCP 8888)--> [Bob]
[Alice] <--(4. Accept: TCP 8888)-- [Server] <--(3. Accept: TCP 8888)--- [Bob]
====================== بدء تدفقات الوسائط المباشرة ======================
[Alice] <==== (UDP 8889 + Fallback TCP عبر السيرفر) ====> [Bob]
```

---

### 3.6 محرك الصوت عالي النقاء HD ومنع أخطاء الذاكرة (Ring Buffers)
* **ترقية HD Voice:** يعمل بتردد `16000 Hz, 16-bit Mono (1280 bytes per 40ms)`.
* **الحماية من انهيار الذاكرة `0xc0000005`:** استخدام **8 مخازن تشغيل دائرية مثبتة مسبقاً (Pre-Pinned Ring Buffers)** دون أي حجز ديناميكي للذاكرة أثناء المكالمة:

```csharp
// تشغيل الصوت في AudioEngine.cs بدون أي حجز للذاكرة
lock (_outLock)
{
    int idx = _outBufferIndex;
    _outBufferIndex = (_outBufferIndex + 1) % OutBufferCount; // المخزن الدائري

    Buffer.BlockCopy(pcmData, 0, _outBuffers[idx], 0, pcmData.Length);
    waveOutWrite(_hWaveOut, _pOutHeaders[idx], Marshal.SizeOf<WAVEHDR>());
}
```

---

### 3.7 محرك التقاط الفيديو ومعالجة الصور المباشرة
* التقاط الفيديو عبر مكتبة DirectShow من طبقة النواة وضغط كل إطار بصيغة JPEG بجودة 60% لتقليل استهلاك النطاق الترددي للشبكة:

```csharp
// التقاط وضغط إطارات الفيديو في VideoEngine.cs
using var ms = new MemoryStream();
frame.Save(ms, ImageFormat.Jpeg);
byte[] compressedBytes = ms.ToArray();
```

---

# 4. مصفوفة توزيع الأدوار أثناء المناقشة (خطة الـ 4 متحدثين)

```text
┌──────────────────────────────────────────────────────────────────────────────────────────┐
│                         خريطة توزيع الأدوار الهندسية (فريق الـ 4 متحدثين)                 │
├──────────────────────────┬──────────────────────────┬────────────────────────────────────┤
│ المتحدث الأول            │ المتحدث الثاني           │ المتحدث الثالث                     │ المتحدث الرابع
│ مهندس المعمارية والشبكات │ مهندس الأمن والمصادقة    │ مهندس الخادم وقواعد البيانات       │ مهندس الميديا وإدارة الذاكرة
│ (Lead Network Architect) │ (Cyber Security & Auth)  │ (Backend & Database Engineer)      │ (Real-Time Media & Systems)
└──────────────────────────┴──────────────────────────┴────────────────────────────────────┴────────────────────────────────────┘
```

---

### 👤 المتحدث الأول: مهندس المعمارية والشبكات وبروتوكول التأطير (Lead Network & Protocol Architect)
* **المسؤوليات والعرض التقديمي:**
  1. **افتتاح العرض:** تقديم مشكلة المشروع (الحاجة لنظام اتصالات محلي آمن ومستقل عن الإنترنت) وأهدافه الهندسية.
  2. **معمارية النظام العامة:** شرح تقسيم النظام وفق Clean Architecture (الخادم، العميل، النواة المشتركة).
  3. **بروتوكول التأطير الثنائي (12-Byte FrameHeader):** شرح تركيبة الـ 12 بايت، الـ Magic Byte `0x53` ('S')، وترتيب بايتات الشبكة Big-Endian.
  4. **حل مشكلة تجزئة ودمج دفق TCP (Fragmentation & Merging):** شرح آلة الحالة `FrameStreamParser` وكيف تضمن قراءة الحزم بدقة.
  5. **خدمة الاكتشاف التلقائي لشبكات الواي فاي (UDP Auto-Discovery):** شرح آلية البث على المنفذ `8887` والاتصال التلقائي بدون كتابة عنوان IP.
* **الملفات التي يركز عليها:** `FrameHeader.cs`, `FrameStreamParser.cs`, `ServerDiscovery.cs`, `SyncPacket.cs`.

---

### 👤 المتحدث الثاني: مهندس الأمن السيبراني والمصادقة وإدارة الجلسات (Cyber Security & Auth Engineer)
* **المسؤوليات والعرض التقديمي:**
  1. **محرك تجزئة كلمات المرور الآمن (PBKDF2/SHA-256):** شرح آلية التشفير باستخدام الـ `128-bit Salt` العشوائي المشفر، و 100,000 دورة تكرار، والمقارنة الزمنية الثابتة `FixedTimeEquals` لمنع هجمات التوقيت وجداول قوس قزح.
  2. **إدارة رموز الجلسات (JWT Tokens RFC 7519):** شرح توليد والتحقق من التوكنات الموقعة بـ HMAC-SHA256، وتجنب فحص قاعدة البيانات في كل حزمة.
  3. **الأمان ومكافحة حقن البيانات (Input Sanitization & Injection Defense):** شرح حماية قاعدة البيانات عبر Parameterized SQL Queries، وحماية الشبكة بضبط سقف الحزم على 10MB لمنع هجمات حجب الخدمة (DoS).
  4. **سجلات التدقيق والأمان المؤسسي (Audit Trail):** شرح جدول `SERVER_AUDIT_LOGS` وتسجيل أحداث المصادقة وعناوين IP وفق معايير ISO 27001.
* **الملفات التي يركز عليها:** `CryptoEngine.cs`, `JwtTokenEngine.cs`, `UserRepository.cs`, `AuditLogRepository.cs`.

---

### 👤 المتحدث الثالث: مهندس الخادم وقواعد البيانات والمزامنة (Backend & Database Engineer)
* **المسؤوليات والعرض التقديمي:**
  1. **محرك خادم المقابس (TcpSocketServer Port 8888):** شرح معالجة الاتصالات المتزامنة بنمط غير حاجب (`async/await`) وتطبيق نمط الحواجز المقسمة `Bulkhead Pattern` لعزل استثناءات العملاء.
  2. **قاعدة البيانات المركزية (SQLite 3NF Schema & WAL Mode):** شرح الجداول السبعة، مفاتيح الربط `Foreign Keys`، تشغيل `WAL Mode` للقراءة والكتابة المتزامنة بدون أقفال، والفهارس الذكية.
  3. **محرك المزامنة التلغرامي وطوابير الانتظار (Offline Queue):** شرح دورة حياة الرسائل وحالات التسليم (✓ / ✓✓ / ✓✓ زرقاء) ودفع الرسائل المعلقة فور عودة اتصال المستلم.
  4. **مكرر الوسائط المباشر (UdpMediaRelay Port 8889):** شرح كيفية قيام الخادم بتمرير حزم الصوت والفيديو بين أطراف المكالمة بأقل تأخير زمني.
* **الملفات التي يركز عليها:** `TcpSocketServer.cs`, `DatabaseManager.cs`, `MessageRepository.cs`, `UdpMediaRelay.cs`, `SessionManager.cs`.

---

### 👤 المتحدث الرابع: مهندس الميديا وإدارة الذاكرة والأنظمة (Real-Time Media & Systems Engineer)
* **المسؤوليات والعرض التقديمي:**
  1. **محرك الصوت عالي النقاء HD وحماية الذاكرة (AudioEngine):** شرح معيار 16kHz HD Voice ونظام المخازن الدائرية الثابتة `Zero-Allocation Pre-Pinned Ring Buffers` والقضاء التام على خطأ `0xc0000005`.
  2. **محرك التقاط الفيديو والمعاينة الذاتية (VideoEngine & PiP):** شرح التقاط الكاميرا بمكتبة AForge، إطفاء ضوء الكاميرا الحقيقي (LED) عند الكتم، نافذة المعاينة الذاتية في الزاوية (PiP)، والبث الهجين (UDP+TCP).
  3. **إلغاء تكرار الإطارات (Bitwise Deduplication):** شرح خوارزمية دمج دفق UDP و TCP وإلغاء تكرار الإطارات المتطابقة عبر مفتاح `((long)Type << 32) | Seq`.
  4. **إدارة دورة حياة المقابس والموارد (Deterministic Resource Disposal):** شرح تطبيق نمط `IDisposable` وتحرير مقابض بطاقات الصوت والكاميرا لمنع تسرب الذاكرة (Memory Leaks).
* **الملفات التي يركز عليها:** `AudioEngine.cs`, `VideoEngine.cs`, `MediaStreamService.cs`, `MessageItem.cs`, `CallCoordinator.cs`.

---

# 5. بنك الأسئلة التقنية العميقة المتوقعة وإجاباتها النموذجية

### أ. أسئلة بروتوكولات الشبكة وهندسة المقابس (Networking & Sockets)

#### س1: لماذا استخدمتم كلاً من بروتوكول TCP وبروتوكول UDP معاً في النظام ولم تكتفوا بأحدهما؟
> **الإجابة النموذجية (المتحدث الأول):** 
> "استخدمنا المعمارية الهجينة وفق المتطلبات الفيزيائية لكل طبقة:
> 1. **بروتوكول TCP (Port 8888):** للمصادقة، الرسائل النصية، نقل الملفات، وتبادل إشارات المكالمات؛ لأنها تتطلب **موثوقية حتمية (Guaranteed Delivery)** وضمان عدم سقوط أي بايت وتسلسل الحزم الصارم عبر ميزة الـ Acknowledgement والـ Flow Control.
> 2. **بروتوكول UDP (Port 8889):** لتدفقات الصوت والفيديو المباشرة؛ لأن الوسائط الحية لا تتحمل تأخير إعادة الإرسال (Retransmission Overhead) بل تتطلب **أقل زمن انتقال ممكن (Low Latency)**؛ فإذا سقط إطار صوتي، تجاوزه فورا أفضل من تجميد المحادثة."

#### س2: ما هي مشكلة تجزئة حزم TCP (TCP Streaming Fragmentation / Packet Merging) وكيف حللتموها في الكود عبر آلة الحالة؟
> **الإجابة النموذجية (المتحدث الأول):**
> "بروتوكول TCP يعمل كـ Stream مستمر للبايتات على مستوى الـ Kernel Socket Buffer ولا يدرك حدود الرسائل (Message Boundaries). في حالات ضغط الشبكة أو الـ MTU قد تدمج أكثر من رسالة في قراءة واحدة أو تنقسم الرسالة الواحدة على دفعات.
> حللنا ذلك بتصميم **بروتوكول تأطير ثنائي مخصص (12-Byte FrameHeader)** يحدد طول الحمولة `PayloadLength` بدقة ونوع الحزمة `PacketType` بترتيب `Big-Endian`. قمنا ببناء آلة حالة (`FrameStreamParser`) تحتفظ بالمخزن المؤقت وتنتظر اكتمال 12 بايت لاستخراج الطول، ثم تقطع الحزمة بدقة متناهية بمجرد اكتمال الـ Payload المحدد دون أي تداخل."

#### س3: لماذا فرضتم ترتيب الشبكة (Big-Endian Network Byte Order) لترويسة الحزم؟
> **الإجابة النموذجية (المتحدث الأول):**
> "لأن معالجات x86/x64 تعمل بنظام Little-Endian (تخزين البايت الأقل أهمية أولاً)، بينما معالجات ARM وراوترات الشبكة ومعيار الإنترنت القياسي (RFC 1700) تعمل بنظام Big-Endian. لو تم إرسال الأرقام مباشرة (مثل int32 أو short) دون توحيد الترتيب عبر `BinaryPrimitives.WriteInt32BigEndian`، لانعكست قيم أطوال الحزم والأنواع عند اتصال جهاز ذي معمارية مختلفة وانهار الاتصال فورياً."

---

### ب. أسئلة الأمن السيبراني وعلم التشفير (Cyber Security & Cryptography)

#### س4: لماذا استخدمتم خوارزمية PBKDF2/SHA-256 مع Salt و 100,000 دورة بدلاً من التجزئة المباشرة SHA-256 أو MD5؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "دوال التجزئة العادية مثل SHA-256 و MD5 صممت لتكون سريعة جداً لمعالجة الملفات، مما يجعلها نقطة ضعف خطيرة أمام هجمات التخمين باستخدام كروت الشاشة (GPU Brute-Force Cracking) حيث يمكن اختبار مليارات التخمينات في الثانية.
> خوارزمية **PBKDF2 مع 100,000 دورة تكرار** تُعرف بـ (Key Stretching Algorithm) وتجعل عملية حساب التجزئة الواحدة تستهلك وقتاً معالجياً مدروساً (مما يبطئ هجمات المخترق بملايين المرات)، ومع إضافة الـ `128-bit Cryptographic Salt` العشوائي لكل مستخدم، تصبح هجمات الجداول المسبقة (Rainbow Tables) مستحيلة رياضياً."

#### س5: ما هي هجمات التوقيت (Timing Attacks) وكيف تحمي دالة `FixedTimeEquals` النظام منها أثناء المصادقة؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "دوال مقارنة النصوص العادية (مثل `==` أو `.Equals()`) تقارن الحروف حرفاً بحرف وتتوقف فور مصادفة أول حرف غير متطابق (Early Exit). هذا التوقف المبكر يسبب فارقاً زمنياً ميكروسكوبياً في استجابة الخادم يمكن للمهاجم قياسه لمعرفة الحروف الصحيحة تدريجياً.
> قمنا بتطبيق دالة **المقارنة الزمنية الثابتة (`FixedTimeEquals`)** التي تقوم بإجراء عملية XOR لكافة بايتات الهاش بالكامل دائماً ولا تخرج مبكراً، مما يضمن زمناً متطابقاً تماماً في كافة محاولات التحقق ويبطل هجمات التوقيت بنسبة 100%."

#### س6: كيف تمنع الاستعلامات المعلمنة (Parameterized SQL Queries) هجمات حقن SQL (SQL Injection) على مستوى المحرك؟
> **الإجابة النموذجية (المتحدث الثاني):**
> "عند استخدام دمج النصوص المباشر (String Concatenation)، يقوم مترجم SQL بمعاملة مدخلات المستخدم كأوامر تنفيذية.
> أما عند استخدام Parameterized Queries في SQLite، يقوم المحرك بمرحلة **التحليل النحوي والتجميع (Pre-Compilation / Query Plan)** أولاً للهيكل الثابت للاستعلام، ثم تُمرر مدخلات المستخدم كقيم مجردة (Literal Values) في مصفوفة البارامترات، مما يستحيل معه تعديل شجرة الإعراب (Parse Tree) أو تنفيذ أي أمر خبيث مهما احتوت المدخلات على علامات اقتباس أو شفرات SQL."

---

### ج. أسئلة الخادم والتزامن وقواعد البيانات (Concurrency, Server & DB Engine)

#### س7: كيف يتعامل خادم المقابس (TcpSocketServer) مع مئات العملاء المتزامنين دون استهلاك خيط لكل عميل (Thread Starvation)؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "من خلال استخدام **البرمجة غير الحاجبة القائمة على منافذ إكمال الإدخال والإخراج (I/O Completion Ports - IOCP)** عبر استدعاءات `.AcceptTcpClientAsync()` و `.ReadAsync()`. الخادم لا يحجز خيطاً حقيقياً لكل اتصال أثناء انتظار وصول البيانات، بل يتحرر الخيط ويعود لمصفوفة الخيوط (`ThreadPool`)، وعندما ترسل بطاقة الشبكة إشارة مقاطعة (Hardware Interrupt) بجهوزية البيانات، يستيقظ أحد خيوط النظام لمعالجة الحزمة فقط، مما يتيح خدمة آلاف الاتصالات بأقل عدد من الخيوط وبدون أي استهلاك زائد للذاكرة."

#### س8: ما هو وضع WAL (Write-Ahead Logging) في SQLite وما فائدته الجوهرية في هذا النظام؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "في الوضع التقليدي (Rollback Journal)، تقوم SQLite بقفل كامل قاعدة البيانات (Database Exclusive Lock) أثناء الكتابة، مما يمنع جميع عمليات القراءة حتى انتهاء المعاملة ويسبب تأخيراً وتجميداً.
> بتفعيل وضع **WAL Mode** عبر `PRAGMA journal_mode = WAL;`، تتم كتابة التعديلات في ملف سجل منفصل (`.wal`)، مما يتيح **عمليات قراءة غير متزامنة لا نهائية في نفس لحظة الكتابة** (Readers do not block Writers, and Writers do not block Readers)، وهو ما يضمن سرعة مزامنة فائقة للرسائل وتحديث حالات التسليم."

#### س9: كيف يحمي نمط الحواجز المقسمة (Bulkhead Pattern) الخادم من الانهيار عند حدوث أخطاء غير متوقعة في أحد المتصلين؟
> **الإجابة النموذجية (المتحدث الثالث):**
> "كل جلسة عميل `ClientSession` تُدار في مهمة غير متزامنة مستقلة محاطة بكتلة `try-catch-finally` معزولة تماماً. فإذا انفصل اتصال العميل فجأة، أو أرسل حزم بيانات تالفة، أو حدث استثناء شبكي، يتم اعتراض الخطأ محلياً وتحرير المقبس والذاكرة الخاصة بهذا العميل بأمان وإشعار `SessionManager` بإلغاء تسجيله، دون أن يتأثر الخادم المركزي أو بقية المتصلين إطلاقاً."

---

### د. أسئلة أنظمة التشغيل وإدارة الذاكرة والميديا (OS, Memory, P/Invoke & Streaming)

#### س10: ما كان سبب خطأ الذاكرة `0xc0000005 (STATUS_ACCESS_VIOLATION)` وكيف تم القضاء عليه بنظام المخازن الدائرية الثابتة؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "خطأ `0xc0000005` هو خطأ انتهاك وصول الذاكرة (Access Violation) في نواة ويندوز. كان يحدث عند استخدام مؤشرات `WAVEHDR` عبر WinMM P/Invoke وتحرير الذاكرة أو تحرير تثبيت المقبض (`GCHandle.Free`) في خيط خلفي بينما بطاقة الصوت وقناة الـ DMA لا تزالان تقرآن الذاكرة المخصصة للمقطع الصوتي.
> قمنا بالقضاء على المشكلة نهائياً بابتكار **هندسة المخازن الدائرية الثابتة (Zero-Allocation Pre-Pinned Ring Buffers)**؛ حيث يتم حجز وتثبيت 8 مخازن بحجم ثابت في الذاكرة عبر `GCHandleType.Pinned` مرة واحدة فقط عند بدء المكالمة، وعند تشغيل أي مقطع يتم نسخ البايتات فقط عبر `Buffer.BlockCopy` داخل المخزن الدائري التالي دون أي حجز أو تحرير ديناميكي أثناء المكالمة، مما منع الخطأ بنسبة 100% وخفض استهلاك المعالج إلى الصفر."

#### س11: كيف يعمل نظام البث الهجين (Hybrid Dual-Channel UDP + TCP) وإلغاء تكرار الإطارات (Deduplication)؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "بعض شبكات الواي فاي المؤسسية تطبق جدران حماية تحظر حزم UDP أو تعزل الأجهزة (Client Isolation). قمنا بتصميم ناقل هجين يبث حزم الوسائط عبر UDP و TCP بالتوازي.
> ولتجنب تشغيل الصوت أو عرض الفيديو مرتين، قمنا ببناء خوارزمية إلغاء التكرار اللحظي عبر توليد مفتاح إطار ثنائي بحجم 64-بت:
> `long frameKey = ((long)mediaFrame.FrameType << 32) | (uint)mediaFrame.SequenceNumber;`
> حيث يتم فحص المفتاح عبر `ConcurrentDictionary` فإذا وصل الإطار عبر UDP أولاً يتم تشغيله فوراً ورفض الحزمة القادمة عبر TCP، والعكس صحيح، مما يضمن استمرارية المكالمة بأعلى جودة تحت أي بيئة شبكية."

#### س12: كيف تتعامل مكتبة DirectShow و AForge مع مقابض الكاميرا فيزيائياً ولماذا ينطفئ ضوء الكاميرا (LED) عند الكتم؟
> **الإجابة النموذجية (المتحدث الرابع):**
> "تتعامل مكتبة DirectShow مع برنامج تشغيل الكاميرا عبر واجهات COM ومقابض النواة (Kernel Streaming Handles).
> بدلاً من الاكتفاء بإخفاء صورة الكاميرا برمجياً في الواجهة كما تفعل بعض التطبيقات الضعيفة، يقوم نظامنا باستدعاء `_videoSource.SignalToStop()` وتحرير مقبض التقاط الفيديو في نظام التشغيل، مما يجعل شريحة الكاميرا تغلق الدائرة الكهربائية وينطفئ ضوء الـ LED الحقيقي على اللابتوب فورياً لحماية خصوصية المستخدم، ثم يعاد تشغيل المقبض بسلاسة عند إلغاء الكتم."

---

### هـ. أسئلة المقارنة والقرارات المعمارية (Architectural Trade-offs)

#### س13: لماذا بنيتم بروتوكولكم الخاص على المقابس الخام (Raw Sockets) ولم تستخدموا SignalR أو WebRTC الجاهزة؟
> **الإجابة النموذجية (يشترك فيها الفريق):**
> "1. **التحكم الهندسي والسيادة الكاملة:** بناء البروتوكول على Raw Sockets منحنا فهماً دقيقاً لحركة البايتات وحجم الترويسة (12 بايت فقط)، وتجنب العبء الزائد (Overhead) لبروتوكولات WebSocket و HTTP Handshakes في SignalR.
> 2. **العمل في الشبكات المعزولة ذاتياً (Air-Gapped & Offline LAN):** مكتبات WebRTC تتطلب خوادم STUN/TURN/ICE خارجية للـ NAT Traversal وتفشل بدون اتصال خارجي، بينما مكرر الوسائط الخاص بنا (UDP Media Relay) مدمج داخل خادمنا المحلي ويعمل فورياً داخل شبكة الواي فاي بدون إنترنت.
> 3. **الكفاءة واستهلاك الموارد:** حزمنا الثنائية مباشرة وسريعة ولا تتطلب استهلاك معالج أو ذاكرة في عمليات التحويل والتغليف المعقدة."

---

# 6. نصائح ذهبية وسيناريو العرض الحي (Live Demo Playbook)

### 🎬 سيناريو العرض العملي الحي خطوة بخطوة أمام اللجنة:

1. **التهيئة (قبل الدخول):**
   - توصيل لابتوبين على نفس شبكة الواي فاي أو نقطة اتصال الهاتف (Hotspot).
2. **بدء العرض (المتحدث الأول):**
   - تشغيل `SyncPulse.Server` واستعراض لوحة التحكم الإدارية.
   - تشغيل `SyncPulse.Client` واستعراض **الاكتشاف التلقائي الفوري للسيرفر عبر منفذ 8887** دون كتابة IP.
3. **المصادقة والأمان (المتحدث الثاني):**
   - تسجيل حسابين جديدين وتسجيل الدخول، واستعراض توليد الـ Salt والـ JWT وظهور المستخدم في لوحة الخادم.
4. **المراسلة والمزامنة (المتحدث الثالث):**
   - إرسال رسائل نصية واستعراض علامات الصح (✓ أُرسلت / ✓✓ استُلمت / ✓✓ قُرئت).
   - إغلاق العميل الثاني، إرسال رسالة، ثم إعادة فتحه لإظهار **وصول الرسائل المعلقة من الـ Offline Queue فورياً**.
5. **الوسائط والمكالمات الحية (المتحدث الرابع):**
   - إرسال صورة وملف صوتي واستعراض معالجة الوسائط ونقلها.
   - بدء مكالمة فيديو، استعراض **المعاينة الذاتية (PiP)** ونقاء صوت HD (16kHz).
   - الضغط على زر كتم الكاميرا وإظهار **انطفاء ضوء الكاميرا الحقيقي (LED)** على اللابتوب.
   - إنهاء المكالمة وإظهار **بقاء التطبيق مفتوحاً دون إغلاق**.

---
**وفقكم الله وحقق لكم أعلى الدرجات ومراتب الشرف والامتياز! 🎓🌟🚀**
